# This project requires devDependencies to build/test/typecheck.
# The shell environment has NODE_ENV=production which makes npm omit dev deps.
# Force-include them so `npm install` is always complete.
include=dev
